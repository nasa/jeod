\boilerplatechapterone{%
  The \ModelDesc provides:
  \begin{itemize}
  \item A generic framework for defining checkpointable/restartable objects,
  \item Checkpointable/restartable replacements for the
    Standard Template Library (STL) containers used within JEOD
    (list, vector, and set), and
  \item Mechanisms to checkpoint and restart these STL containers.
\end{itemize}
}%
{%
  \ModelHistory
}
