/*
 * gauss_jackson_state_machine_ut.cc
 */

#include "message_handler_mock.hh"
#include "utils/integration/gauss_jackson/include/gauss_jackson_state_machine.hh"

#include "gmock/gmock.h"
#include "gtest/gtest.h"
using testing::_;
using testing::AnyNumber;
using testing::Mock;

using namespace jeod;

TEST(GaussJacksonStateMachine, create) {}

TEST(GaussJacksonStateMachine, state_name) {}

TEST(GaussJacksonStateMachine, configure) {}

TEST(GaussJacksonStateMachine, reset) {}

TEST(GaussJacksonStateMachine, set_bootstrap_edit_redo_needed) {}

TEST(GaussJacksonStateMachine, perform_step) {}

TEST(GaussJacksonStateMachine, transition_state) {}

TEST(GaussJacksonStateMachine, exit_priming) {}

TEST(GaussJacksonStateMachine, exit_bootstrap_edit) {}

TEST(GaussJacksonStateMachine, exit_bootstrap_step) {}
