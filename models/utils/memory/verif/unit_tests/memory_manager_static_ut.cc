/*
 * memory_manager_static_ut.cc
 */

#include "utils/memory/include/memory_manager.hh"

#include "gmock/gmock.h"
#include "gtest/gtest.h"

using namespace jeod;

TEST(JeodMemoryManager, check_master) {}

TEST(JeodMemoryManager, set_debug_level) {}

TEST(JeodMemoryManager, set_guard_enabled) {}

TEST(JeodMemoryManager, is_table_empty) {}

TEST(JeodMemoryManager, register_class) {}

TEST(JeodMemoryManager, get_type_descriptor) {}

TEST(JeodMemoryManager, create_memory) {}

TEST(JeodMemoryManager, is_allocated) {}

TEST(JeodMemoryManager, destroy_memory) {}

TEST(JeodMemoryManager, register_container) {}

TEST(JeodMemoryManager, deregister_container) {}

TEST(JeodMemoryManager, set_mode) {}
